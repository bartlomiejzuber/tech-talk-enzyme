import React from 'react';
import { mount } from 'enzyme';
import { Menu } from '../components/Menu';
import { twoPierogis, currentOrderItems } from '../fixtures';

describe('<Menu />', () => {
  it('renders the menu with two pierogis & one already in the basket', () => {
    
  });
});
